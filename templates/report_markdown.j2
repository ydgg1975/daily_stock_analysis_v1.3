{% from '_macros.j2' import render_field_group with context %}
# 🎯 {{ report_date }} {{ labels.dashboard_title }}

> {{ labels.analyzed_prefix }} **{{ results|length }}** {{ labels.stock_unit }} | 🟢{{ labels.buy_label }}:{{ buy_count }} 🟡{{ labels.watch_label }}:{{ hold_count }} 🔴{{ labels.sell_label }}:{{ sell_count }}
> {{ labels.report_title }}
> 报告生成时间（北京时间）: `{{ report_generated_at_bjt or na('接口未返回') }}`
> 市场时间（原始）: `{{ market_timestamp or na('接口未返回') }}`
> 市场时间（北京时间）: `{{ market_timestamp_bjt or na('接口未返回') }}`
> 交易日: `{{ market_session_date or na('接口未返回') }}`
> 会话类型: `{{ session_type or na('接口未返回') }}`
> 新闻发布时间（北京时间）: `{{ news_published_at_bjt or na('接口未返回') }}`

## 📊 {{ labels.summary_heading }}

{% for e in enriched %}
{{ e.signal_emoji }} **{{ e.stock_name }}({{ e.result.code }})**: {{ e.localized_operation_advice }} | {{ labels.score_label }} {{ e.result.sentiment_score }} | {{ e.localized_trend_prediction }}
{% endfor %}

{% if not summary_only %}

---
{% for e in enriched %}
{% set result = e.result %}
{% set std = e.standard_report %}
## {{ e.signal_emoji }} {{ e.stock_name }} ({{ result.code }})

### 1. 标题区 / Title
- **股票名 / ticker**: {{ std.title.stock }}
- **评分**: {{ std.title.score if std.title.score is not none else na('接口未返回') }}
- **买入 / 观望 / 卖出**: {{ std.title.operation_advice }}
- **看多 / 看空 / 震荡**: {{ std.title.trend_prediction }}
- **一句话决策**: {{ std.title.one_sentence }}
- **时效性**: {{ std.title.time_sensitivity }}

### 2. 重要信息速览 / Key Updates
{{ render_field_group(std.info_fields) }}

### 3. 持仓建议 / Position Advice
- **空仓者建议**: {{ std.position_advice.no_position }}
- **持仓者建议**: {{ std.position_advice.has_position }}

### 4. 当日行情（常规交易时段） / Market (Regular Session)
{{ render_field_group(std.market.regular_fields) }}

### 4.1 扩展时段（盘前/盘后） / Extended Session
{{ render_field_group(std.market.extended_fields) }}
{% if std.market.consistency_warnings %}
- **口径校验**: {{ std.market.consistency_warnings | join('；') }}
{% endif %}

### 5. 技术面 / Technicals
{{ render_field_group(std.technical_fields) }}

### 6. 基本面 / 财报 / 情绪
#### 6.1 基本面
{{ render_field_group(std.fundamental_fields) }}

#### 6.2 财报
{{ render_field_group(std.earnings_fields) }}

#### 6.3 情绪
{{ render_field_group(std.sentiment_fields) }}

### 7. 作战计划 / Battle Plan
{{ render_field_group(std.battle_fields) }}
{% if std.battle_warnings %}
- **风险提示**: {{ std.battle_warnings | join('；') }}
{% endif %}

### 8. 检查清单 / Checklist
{% for item in std.checklist %}
- {{ item }}
{% endfor %}

---
{% endfor %}
{% endif %}
