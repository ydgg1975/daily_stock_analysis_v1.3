I will configure the local environment and optimize the output format as requested.

### 1. Configure Environment Variables
- Update `.env` file with the provided values:
  - `STOCK_LIST`: `002465,002085,600602,601868,601985,603123,000630,002152,002456,002594`
  - `GEMINI_API_KEY`: `AIzaSyDZbGGBriBrqIIxxYeMiXEyX3Y0u3v-hzk`
  - `CUSTOM_WEBHOOK_URLS`: `https://oapi.dingtalk.com/robot/send?access_token=...`

### 2. Optimize Output Format (Table View)
- Modify `src/notification.py`:
  - Import `tabulate` library (already installed).
  - Add a new method `generate_console_table(results)` to `NotificationService`.
  - The table will include key columns: **Code**, **Name**, **Price**, **Change%**, **Advice**, **Score**, **Trend**, **Risk/Reward**.
  - Use `tabulate` with `grid` or `github` format for clean console output.

### 3. Enable Local Debug Output
- Modify `src/core/pipeline.py`:
  - In the `run` method, after collecting all analysis results, call the new `generate_console_table` method.
  - Print the table directly to the console (logger) so you can see the structured analysis results immediately during local debugging.

### Verification
- Run a test analysis (e.g., `python main.py --dry-run` or with a small subset) to verify the table output and environment configuration.
