好的，我将修改本地调试的输出逻辑：在本地生成并自动打开 Markdown 文件，同时保持 GitHub Actions 环境简洁。

### 1. 修改 `src/core/pipeline.py`
- 移除上一轮添加的“控制台打印表格”代码。
- 增加环境检测逻辑：通过 `os.getenv("GITHUB_ACTIONS")` 判断当前运行环境。
- 在 `run` 方法中实现以下逻辑：
  - **如果是本地调试（非 GitHub Actions）：**
    - 调用 `self.notifier.generate_dashboard_report(results)` 生成完整 Markdown 报告。
    - 将报告保存到 `reports/` 目录，文件名包含时间戳（如 `analysis_report_20260310_103000.md`）。
    - 使用系统命令（macOS 下为 `open`）自动打开生成的 Markdown 文件。
  - **如果是 GitHub Actions 环境：**
    - 跳过文件生成和自动打开步骤。
    - 仅保留原有的通知推送逻辑。

### 2. 验证输出
- 在本地运行 `python main.py --no-notify` 进行验证（无需 `--stocks` 参数，直接读取 `.env` 中配置的 `STOCK_LIST`）：
  - 确认控制台不再输出表格。
  - 确认 `reports/` 目录下生成了新的 Markdown 文件。
  - 确认文件会自动打开。
