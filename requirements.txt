# ===================================
# A-Share Stock Selection Intelligent Analysis System - Dependency List
# ===================================

# Core Dependencies
python-dotenv>=1.0.0        # Environment variable configuration management
tenacity>=8.2.0             # Retry mechanism (Exponential backoff)
sqlalchemy>=2.0.0           # ORM database operations
schedule>=1.2.0             # Scheduled task scheduling
exchange-calendars>=4.5.0   # Trading calendars (A-Share/HK/US, Issue #373)

# Data Source Dependencies (Multi-source Strategy, sorted by priority)
efinance>=0.5.5             # Priority 0: Eastmoney Data Source (Highest Priority) https://github.com/Micro-sheep/efinance
akshare>=1.12.0             # Priority 1: Eastmoney Web Scraper Data Source
tushare>=1.4.0              # Priority 2: Tushare Pro API
pytdx>=1.72                 # Priority 2: TongDaXin Market Servers
baostock>=0.8.0             # Priority 3: Baostock Data Source
yfinance>=0.2.0             # Priority 4: Yahoo Finance (Fallback)
longbridge>=0.2.0           # Priority 5: Longbridge OpenAPI (US/HK Fallback)
tickflow>=0.1.0             # TickFlow official SDK (Issue #632, market review enhancement)

# Feishu (Lark)
lark-oapi>=1.0.0            # Feishu API

# Data Processing
pandas>=2.0.0               # Data analysis
pypinyin>=0.50.0            # Name-to-code resolver (pinyin matching)
openpyxl>=3.1.0             # Excel (.xlsx) parsing for import
numpy>=1.24.0               # Numerical computing
json-repair>=0.55.1         # JSON Repair

# AI Analysis
# Keep the historical minimum version while excluding quarantined builds and avoiding future major breaks.
litellm>=1.80.10,!=1.82.7,!=1.82.8,<2.0.0   # Unified LLM client (Gemini/Anthropic/OpenAI/DeepSeek etc.)
tiktoken>=0.8.0,!=0.12.0,<0.14.0            # BPE tokenizer; avoid 0.12.0 plugin regression while allowing Python 3.14 wheels
openai>=1.0.0               # OpenAI SDK (transitive dependency of litellm, kept explicit)
PyYAML>=6.0                 # YAML parser for LITELLM_CONFIG support

# Search Engines (used for fetching stock news)
tavily-python>=0.3.0        # Tavily Search API (1000 free queries/month)
google-search-results>=2.4.0  # SerpAPI (100 free queries/month)

# Network Requests
requests>=2.31.0            # HTTP Requests
markdown2>=2.4.0            # Markdown to HTML converter
imgkit>=1.2.0               # Markdown to Image converter (requires wkhtmltopdf)
fake-useragent>=1.4.0       # Random User-Agent (Anti-blocking)
httpx[socks]                # HTTP client + SOCKS proxy support (Optional for OpenAI)
dingtalk-stream >= 0.24.3   # DingTalk Stream SDK

# Database
# SQLite is built into Python; no extra installation is required.

# Discord Bot
discord.py>=2.0.0           # Discord Bot Development Library
PyNaCl>=1.5.0               # Discord Interaction/Webhook Ed25519 signature verification

# Web Content Extraction
newspaper3k>=0.2.8          # Article extraction
lxml_html_clean             # Fix for lxml.html.clean ImportError in newer lxml versions

# Report Engine
jinja2>=3.1.0               # Jinja2 template engine for report rendering

# FastAPI Web Framework
fastapi>=0.109.0            # Modern Python Web Framework
uvicorn[standard]>=0.27.0   # ASGI Server
python-multipart>=0.0.6     # FastAPI File/Form upload support
