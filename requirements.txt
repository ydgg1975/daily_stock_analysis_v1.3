# ===================================
# Daily Stock Analysis - Dependencies
# ===================================

# Core
python-dotenv>=1.0.0
tenacity>=8.2.0
sqlalchemy>=2.0.0
schedule>=1.2.0
exchange-calendars>=4.5.0

# Data sources (multi-source strategy, by priority)
efinance>=0.5.5             # Priority 0: East Money
akshare>=1.12.0             # Priority 1: East Money crawler
tushare>=1.4.0              # Priority 2: Tushare Pro API
pytdx>=1.72                 # Priority 2: TDX server
baostock>=0.8.0             # Priority 3: BaoStock
yfinance>=0.2.0             # Priority 4: Yahoo Finance (Fallback)
tickflow>=0.1.0             # TickFlow official SDK (Issue #632)

# Feishu / Lark
lark-oapi>=1.0.0

# Data processing
pandas>=2.0.0
pypinyin>=0.50.0            # Name-to-code resolver (pinyin matching)
openpyxl>=3.1.0             # Excel (.xlsx) parsing for import
numpy>=1.24.0
json-repair>=0.55.1

# AI analysis
litellm>=1.80.10,<1.82.7   # Unified LLM client
tiktoken>=0.8.0,<0.12.0    # BPE tokenizer for LLM token counting (#537)
openai>=1.0.0               # OpenAI SDK (transitive dep of litellm)
PyYAML>=6.0                 # YAML parser for LITELLM_CONFIG support

# Search engines (stock news)
tavily-python>=0.3.0        # Tavily Search API
google-search-results>=2.4.0  # SerpAPI

# Network / HTTP
requests>=2.31.0
markdown2>=2.4.0
imgkit>=1.2.0               # Markdown to image (requires wkhtmltopdf)
fake-useragent>=1.4.0
httpx[socks]                # HTTP client + SOCKS proxy support
dingtalk-stream >= 0.24.3   # DingTalk Stream SDK

# Database
# SQLite is built-in, no extra install needed

# Discord bot
discord.py>=2.0.0
PyNaCl>=1.5.0               # Discord Ed25519 signature verification

# Web Content Extraction
newspaper3k>=0.2.8
lxml_html_clean
python-docx>=0.8.11         # DOCX file parsing (recommendation feature)

# Report Engine
jinja2>=3.1.0               # Jinja2 template engine

# FastAPI Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0   # ASGI server
python-multipart>=0.0.6     # FastAPI File/Form upload support
